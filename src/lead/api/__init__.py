"""The pip-facing contracts of LEAD.

Import from the submodules directly: :mod:`lead.api.py123d_log_api` for the
log layout (no simulator or torch needed), :mod:`lead.api.scene_data` and
:mod:`lead.api.scene_loading_spec` for the scene one log tick is read into,
:mod:`lead.api.array_types` for the array layouts of those modalities,
:mod:`lead.api.abstract_policy` and :mod:`lead.api.training_sample` for the
policy contracts, :mod:`lead.api.abstract_driving_agent` for CARLA evaluation.
"""
